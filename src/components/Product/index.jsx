import React from 'react'

const Product = ({item}) => {
  const {
    image,
    title,
    imageSub,
    rating,
    ratedVoice,
    monthlyPayment,
    oldPrice,
    newPrice

  } = item  
  return (
    <div>
        <h2>Arzon narxlar{">"}</h2>
    </div>
  )
}

export default Product